import { pageContainer } from "@/app/classnames"

const SearchPage = () => {

    return (
        <div className={pageContainer}>
            <h1>Search Page</h1>
        </div>
    )
}

export default SearchPage